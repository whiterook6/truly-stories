{
  "name": "astro",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && gh-pages -d dist --dotfiles",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^6.4.4"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "typescript": "^6.0.3"
  }
}
