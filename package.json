{
  "name": "astro",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "astro": "astro",
    "build": "astro build",
    "deploy": "npm run generate:downloads && astro build && gh-pages -d dist --dotfiles",
    "dev": "astro dev",
    "generate:downloads": "tsx scripts/generate-downloads.ts",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^6.4.4"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "epub-gen-memory": "^1.1.2",
    "gh-pages": "^6.3.0",
    "gray-matter": "^4.0.3",
    "marked": "^18.0.7",
    "tsx": "^4.23.1",
    "typescript": "^6.0.3"
  }
}
