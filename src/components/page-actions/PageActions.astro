---
import BackLink from './BackLink.astro';
import DownloadButton from './DownloadButton.astro';
import HomeLink from './HomeLink.astro';
import ShareButton from './ShareButton.astro';
import './PageActions.css';

interface Props {
  showHomeButton?: boolean;
  backHref?: string;
  downloadHref?: string;
  onClickShare?: string;
}

const {
  showHomeButton,
  backHref,
  downloadHref,
  onClickShare,
} = Astro.props as Props;
---
<div class="page-actions" role="group" aria-label="Page actions">
  {showHomeButton && <HomeLink />}
  {backHref && <BackLink href={backHref} />}
  {downloadHref && <DownloadButton href={downloadHref} />}
  {onClickShare && <ShareButton onClick={onClickShare} />}
</div>
